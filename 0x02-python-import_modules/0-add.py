def add(a, b):
    """My addition function

    Args:
        a: first integer
        b: second integer

    Returns:
        The return value. a + b
    """
    print("<a {}> + <b {}> = <add(a,b) {}>").format(a,b,(a+b))
